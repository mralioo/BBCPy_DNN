# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: srm_eeg.yaml
  - override /model: baseline_mbcsp.yaml
  - override /callbacks: default.yaml
  - override /trainer: sklearn.yaml
  - override /logger: mlflow.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: [ "baseline" , "multiband", "mbcsp" ]

seed: 12345

data:
  train_subjects_sessions_dict:
    S1:
      - 1
      - 2
  bands: [ [ 10.5, 13 ], [ 21, 26 ], [ 31.5, 39 ] ]

logger:
  mlflow:
    tags: ${tags}
    experiment_name: mbcsp
    run_name: tune